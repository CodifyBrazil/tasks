export type TaskListProps ={
    id: number;
    name: string;
    createAt: string;
    expireAt: string;
    isDone: boolean;
}

export type trashProps = {
    id: number;
    name: string;
    insertAt: string;
    isDone: boolean;
}